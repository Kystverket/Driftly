apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: example
  namespace: fyr-dev-repo
spec:
  dnsNames:
    - example.fyr.svc
  issuerRef:
    kind: Issuer
    name: example-issuer
  secretName: example-tls
