apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  annotations:
    kargo.akuity.io/color: avocado
  labels:
    stage: dev
  name: repo-escape-hatches-dev
  namespace: fyr-kargo
spec:
  promotionTemplate:
    spec:
      steps:
        - task:
            kind: ClusterPromotionTask
            name: push-image
  requestedFreight:
    - origin:
        kind: Warehouse
        name: repo-escape-hatches
      sources:
        direct: true
        stages: []
  vars:
    - name: path
      value: _kargo/fyr/dev/repo/escape-hatches
    - name: image
      value: ghcr.io/kystverket/repo/escape-hatches
    - name: version
      value: ${{imageFrom(vars.image).Tag}}
