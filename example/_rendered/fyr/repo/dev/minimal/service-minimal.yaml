apiVersion: v1
kind: Service
metadata:
  labels:
    service: repo
    app: minimal
    env: dev
    team: fyr
  name: minimal
  namespace: fyr-dev-repo
spec:
  ports:
    - name: http-minimal
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    service: repo
    app: minimal
    env: dev
    team: fyr
  type: ClusterIP
