apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  annotations:
    kargo.akuity.io/color: avocado
  labels:
    stage: dev
  name: repo-full-dev
  namespace: fyr-kargo
spec:
  promotionTemplate:
    spec:
      steps:
        - task:
            kind: ClusterPromotionTask
            name: push-image
  requestedFreight:
    - origin:
        kind: Warehouse
        name: repo-full
      sources:
        direct: true
        stages: []
  vars:
    - name: path
      value: _kargo/fyr/dev/repo/full
    - name: image
      value: ghcr.io/kystverket/repo/example-frontend
    - name: version
      value: ${{imageFrom(vars.image).Tag}}
