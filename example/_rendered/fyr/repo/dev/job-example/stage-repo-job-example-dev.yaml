apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  annotations:
    kargo.akuity.io/color: avocado
  labels:
    stage: dev
  name: repo-job-example-dev
  namespace: fyr-kargo
spec:
  promotionTemplate:
    spec:
      steps:
        - task:
            kind: ClusterPromotionTask
            name: push-image
  requestedFreight:
    - origin:
        kind: Warehouse
        name: repo-job-example
      sources:
        direct: true
        stages: []
  vars:
    - name: path
      value: _kargo/fyr/dev/repo/job-example
    - name: image
      value: ghcr.io/kystverket/repo/job-example
    - name: version
      value: ${{imageFrom(vars.image).Tag}}
