apiVersion: v1
kind: Service
metadata:
  labels:
    service: example
    app: myapp
    env: dev
    team: fyr
  name: myapp
  namespace: fyr-dev-example
spec:
  ports:
    - name: http-myapp
      port: 80
      protocol: TCP
      targetPort: 80
  selector:
    service: example
    app: myapp
    env: dev
    team: fyr
  type: ClusterIP
